{
  "name": "matrix-qa-mongodb",
  "version": "1.0.0",
  "description": "Matrix QA Test Runner with MongoDB integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-mongodb": "echo 'MongoDB installation instructions:' && echo '1. For Ubuntu/Debian: sudo apt-get install -y mongodb' && echo '2. For macOS: brew install mongodb-community' && echo '3. For Windows: download from https://www.mongodb.com/try/download/community'"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}